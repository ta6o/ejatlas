-# coding: utf-8
.row
  .span12
    .hero-unit
      %h1.title Yeni İhtilaf
    .inner
      =form_for :conflict, '/', :method => :get, :class => :form do |f|
        =partial "conflicts/form", :locals => { :f => f }
